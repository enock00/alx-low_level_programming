#include <stdio.h>
/**
 * main - Entry Point
 * Description: "print all single digits of base 10"
 * Return: Always to 0
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
	printf("%d", n);

	printf("\n");

	return (0);
}
